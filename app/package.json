{
  "name": "presidencia-duelo-app",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run editorial-assets:check && npm run portraits:check && vite build",
    "editorial-assets:check": "node scripts/validate-editorial-assets.mjs",
    "portraits:check": "node scripts/validate-portrait-slots.mjs",
    "test": "node --test src/*.test.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "playwright": "1.55.1",
    "vite": "^6.3.5"
  }
}
