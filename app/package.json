{
  "name": "presidencia-duelo-app",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run portraits:check && vite build",
    "portraits:check": "node scripts/validate-portrait-slots.mjs",
    "test": "node --test src/*.test.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "playwright": "^1.55.0",
    "vite": "^6.3.5"
  }
}
