{
  "name": "presidencia-duelo-app",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run portraits:check && vite build && node scripts/verify-card-art-pilot-excluded.mjs",
    "portraits:check": "node scripts/validate-portrait-slots.mjs",
    "test": "node --test src/*.test.js",
    "test:card-art-pilot-form": "node scripts/card-art-pilot-form-smoke.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "playwright": "^1.55.0",
    "vite": "^6.3.5"
  }
}
